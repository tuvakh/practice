{
  "name": "lecture-10",
  "version": "1.0.0",
  "description": "A toy example of a library API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch server.js",
    "prod": "node server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
